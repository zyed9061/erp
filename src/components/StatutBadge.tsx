// Conserve pour les pages de detail (devis, factures) qui l'importaient deja.
// Une seule source de verite : la pastille partagee de `components/ui`.
export { StatutPill as StatutBadge } from "@/components/ui/StatutPill";
